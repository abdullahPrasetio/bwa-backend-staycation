<%- include('../../../partials/header.ejs') %> 

  <!-- Page Wrapper -->
  <div id="wrapper">

    <!-- Sidebar -->
    <%- include('../../../partials/sidebar.ejs') %> 
    <!-- End of Sidebar -->

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">

      <!-- Main Content -->
      <div id="content">

        <!-- Topbar -->
        <%- include('../../../partials/navbar.ejs') %> 
        <!-- End of Topbar -->

        <!-- Begin Page Content -->
        <div class="container-fluid">
          <%- include('../../../partials/alert.ejs') %> 
          <!-- Page Heading -->
          <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
          <!-- End Heading -->
            <nav>
              <div class="nav nav-tabs" id="nav-tab" role="tablist">
                <a class="nav-item nav-link active" id="nav-home-tab" data-toggle="tab" href="#show-feature" role="tab"
                 aria-controls="show-feature" aria-selected="true">Show Feature</a>
                <a class="nav-item nav-link" id="nav-profile-tab" data-toggle="tab" href="#show-activity" role="tab"
                 aria-controls="show-activity" aria-selected="false">Show Activity</a>
              </div>
            </nav>
            <div class="tab-content" id="nav-tabContent">
                <%- include('show_feature.ejs') %> 
                <%- include('show_activity.ejs') %> 
            </div>
            
        </div>
        <!-- /.container-fluid -->

      </div>
      <!-- End of Main Content -->

      <!-- Footer -->
      <%- include('../../../partials/footer.ejs') %> 
      <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->

  </div>
  <!-- End of Page Wrapper -->

  <!-- Scroll to Top Button-->
  <a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
  </a>

 
<%- include('show_modal_activity.ejs') %> 
<%- include('show_modal_feature.ejs') %> 
  <%- include('../../../partials/script.ejs') %> 
  <script>
      $(document).ready(function(){
        $('#dataTableActivity').DataTable();
                $('#dataTable').on('click','.button-update',function(){
            let id=$(this).data('id');
            let name=$(this).data('name');
            let qty=$(this).data('qty');
            
            $('#edit-feature').modal('show');
            $('.id').val(id);
            $('.name').val(name);
            $('.qty').val(qty);

            });

            $('#dataTableActivity').on('click','.button-update',function(){
            let id=$(this).data('id');
            let name=$(this).data('name');
            let type=$(this).data('type');

            $('#edit-activity').modal('show');
            $('.id').val(id);
            $('.name').val(name);
            $('.type').val(type);

            });
      });
      
  </script>

